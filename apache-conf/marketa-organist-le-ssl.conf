<IfModule mod_ssl.c>
<VirtualHost *:443>
    ServerName marketa-organist.com
    ServerAlias www.marketa-organist.com

#    RewriteEngine On
#    RewriteCond %{HTTPS} off
#    RewriteRule ^ https://organomania.cz%{REQUEST_URI}

    # Proxy settings
    ProxyPreserveHost On
    ProxyPass / http://localhost:8080/marketa-prokopovicova/
    ProxyPassReverse / http://localhost:8080/marketa-prokopovicova/

    # Optional: Logging
    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
#RewriteEngine on
#RewriteCond %{SERVER_NAME} =organomania.cz
#RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]


Include /etc/letsencrypt/options-ssl-apache.conf
SSLCertificateFile /etc/letsencrypt/live/marketa-organist.com/fullchain.pem
SSLCertificateKeyFile /etc/letsencrypt/live/marketa-organist.com/privkey.pem
</VirtualHost>
</IfModule>
<IfModule mod_ssl.c>
<VirtualHost *:80>
    ServerName marketa-organist.com
    ServerAlias www.marketa-organist.com

#    RewriteEngine On
#    RewriteCond %{HTTPS} off
#    RewriteRule ^ https://organomania.cz%{REQUEST_URI}

    # Proxy settings
    ProxyPreserveHost On
    ProxyPass / http://localhost:8080/marketa-prokopovicova/
    ProxyPassReverse / http://localhost:8080/marketa-prokopovicova/

    # Optional: Logging
    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
#RewriteEngine on
#RewriteCond %{SERVER_NAME} =organomania.cz
#RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
RewriteEngine on
# Some rewrite rules in this file were disabled on your HTTPS site,
# because they have the potential to create redirection loops.

RewriteCond %{SERVER_NAME} =www.marketa-organist.com [OR]
RewriteCond %{SERVER_NAME} =marketa-organist.com
RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]


</VirtualHost>
</IfModule>
